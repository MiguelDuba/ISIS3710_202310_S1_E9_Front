export const formatterUSD = new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: 'USD',
  });

export const formatterCOP = new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: 'COP',
  });

